# Copy this file to secrets.toml and fill in your Supabase connection string.
# secrets.toml is git-ignored and must NOT be committed.
#
# Get this from Supabase → Project Settings → Database → Connection string (URI mode)

DATABASE_URL = "postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
